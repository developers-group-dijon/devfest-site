---
layout: base.njk
title: "DevFest Dijon"
permalink: "/"
---

<div>
   <div>
      <span>
         {%- if (event.dayCount === 1) -%}
            {{ event.dateStart | dateFormat }}
         {%- else -%}
            du {{ event.dateStart | dateFormat }} au {{ event.dateEnd | dateFormat }}
         {%- endif -%}
      </span>
      <span>Le plus grand festival dédié aux développeurs de Bourgogne-Franche-Comté</span>
      <span>{{ event.location.name }}</span>
   </div>
   <div>
      <div>
      {%- for comment in event.comments %}
         <h2>{{ comment.title }}</h2>
         <div>{{- comment.content | md | safe -}}</div>
      {%- endfor %}
      </div>
      <div>
         <div><span>{{ event.visitors }}</span><span>Participants</span></div>
         <div><span>{{ event.dayCount }}</span><span>Journée</span></div>
         <div><span>{{ event.sessionCount }}</span><span>Sessions</span></div>
         <div><span>{{ tracks.length }}</span><span>Scènes</span></div>
      </div>
   </div>
   <div>
      <h2>TODO: quelques speakers</h2>
   </div>
   <div>
      <h2>Réservez-votre billet</h2>
      <div>
      {% for pricing in ticketing.pricings %}
         <div><a href="{{ pricing.url | default(ticketing.url) }}">{{ pricing.name }}</a></div>
         <div>{{ pricing.price }}</div>
         <div>{{ ticketing.comment }}</div>
         <div>{{ pricing.comment }}</div>
      {% endfor %}
      </div>
   </div>
   <div>
      <h2>TODO: photos édition précédente</h2>
   </div>
   <div>
      <h2>TODO: location (carte + infos)</h2>
   </div>
   <div>
      <img src="{{ event.team.photo | normalizeAssetUrl }}" alt="Photo de l'équipe oragnisatrice">
      <div>
         <h2>Pourquoi un Devfest à Dijon ?</h2>
         <div>{{ event.team.why | md | safe }}</div>
         <a href="{{ (event.previousEditions | first).url }}">Voir l'édition {{ (event.previousEditions | first).year }}</a>
         <h2>Organisateur</h2>
         <div>{{ event.team.message | md | safe }}</div>
         <a href="{# TODO: page de l'équipe /team/ #}">Découvrir l'équipe</a>
      </div>
   </div>
   <div>
      <h2>Partenaires &amp; Sponsors</h2>
      {% for sponsorType in sponsors %}
      <div class="sponsorType">
         <h3>{{ sponsorType.name }}</h3>
         {% for sponsor in sponsorType.sponsors %}
         <a class="sponsor" href="{{ sponsor.website }}" title="{{ sponsor.name }}"><img src="{{ sponsor.logoUrl | normalizeAssetUrl }}" alt="Logo {{ sponsor.name}}"></a>
         {% endfor %}
      </div>
      {% endfor %}
   </div>
</div>